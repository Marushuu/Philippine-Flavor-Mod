phfp_declare_independence_button = {
    name = phfp_declare_independence_button
    desc = phfp_declare_independence_button_desc

    visible = {
        has_journal_entry = phfp_je_propagandist_movement
    }

    possible = {
        OR = {
            je:phfp_je_propagandist_movement = {
                "scripted_bar_progress(phfp_propaganda_movement_bar)" >= 300
            }
            liberty_desire >= 80
        }
    }

    effect = {
        set_variable = force_revolution_var
    }
}
